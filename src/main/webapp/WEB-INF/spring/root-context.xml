<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
	xsi:schemaLocation="http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	
	<!-- Root Context: defines shared resources visible to all other web components -->
	<!-- 의존성 주입 -->

	<!-- hikariCP (JDBC역할 Spring에선 hikariCP사용. JDBC 등록하는 것 처럼)-->
	<bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
		<!-- 일반적인 연결 
		<property name="driverClassName" value="oracle.jdbc.OracleDriver"></property>
		<property name="jdbcUrl" value="jdbc:oracle:thin:@localhost:1521:XE"></property>
		 -->
		 <!-- 콘솔에 테이블 모양으로 찍기 위해 이렇게 설정 -->
		<property name="driverClassName" value="net.sf.log4jdbc.sql.jdbcapi.DriverSpy"></property>
		<property name="jdbcUrl" value="jdbc:log4jdbc:oracle:thin:@localhost:1521:XE"></property>
		<property name="username" value="polite"></property>
		<property name="password" value="polite"></property>
	</bean>
	
	
	<!-- 커넥션 풀  -->
	<!-- destroy-method="close" 닫을 때 close 해줘~ 사용자가 혼자면 열려있어도 되는데 여러명일 경우 닫아줘야된다. -->
	<bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
		<constructor-arg ref="hikariConfig"/>
	</bean>
	
	
	<!-- mybatis 설정, mybatis는 DBCP를 이요하기 때문에 dataSource 받아옴 -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource"></property>
		
	</bean>		
	
	<mybatis-spring:scan base-package="com.SH.mapper"/>
	
	<context:component-scan base-package="com.SH.service" />
	
</beans>
